#!/usr/bin/env python3
"""Returns the shape of a numpy matrix."""


def np_shape(matrix):
    """
    Returns the shape of a numpy matrix.

    Parameters:
    matrix (numpy.ndarray): The matrix to get the shape of.

    Returns:
    tuple: A tuple representing the shape of the matrix.
    """
    return matrix.shape
